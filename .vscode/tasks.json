{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "create-nes-game build && create-nes-game run",
            "problemMatcher": [],

            "group": {
                "kind": "build",
                "isDefault": true,
            }
        }
    ]
}